import React from 'react';

const Highlighted = ({value}) => {
    return (
        <div className='highlighted'>
            <span>{value}</span>
        </div>
    );
}

export default Highlighted;
